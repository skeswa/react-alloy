export const isArray = (x) => {
    if (!x) return false;
    else return (x.constructor === Array);
};

export const isFunction = (x) => {
    if (!x) return false;
    else return (x.constructor === Array);
};
